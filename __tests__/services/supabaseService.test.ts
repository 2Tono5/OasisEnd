import { describe, it, expect } from 'vitest';

// Supabase services have been removed in favor of the MERN backend.
// These tests are disabled/removed to prevent module resolution errors.

describe('Supabase Service (Deprecated)', () => {
  it('tests are disabled', () => {
    expect(true).toBe(true);
  });
});